import type { ItineraryItem } from "../lib/types";
export function ItineraryTimeline({ items }: { items: ItineraryItem[] }) { const days = [...new Set(items.map((item) => item.day))]; return <div className="space-y-4">{days.map((day) => <section key={day} className="card"><h3 className="text-xl font-bold">{day}</h3><div className="mt-3 space-y-3">{items.filter((item) => item.day === day).map((item) => <div key={item.id} className="border-l-4 border-wayfare pl-4"><p className="font-bold">{item.starts_at} {item.title}</p><p className="text-sm text-slate-600">{item.ends_at ? `Ends ${item.ends_at}` : "End time TBD"}</p></div>)}</div></section>)}</div>; }
