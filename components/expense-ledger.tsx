import type { Payment } from "../lib/settlement"; import type { Expense } from "../lib/types";
export function ExpenseLedger({ expenses, payments }: { expenses: Expense[]; payments: Payment[] }) { return <div className="card space-y-4"><h2 className="text-xl font-bold">Expenses & balances</h2>{expenses.map((e) => <div key={e.id} className="flex justify-between border-b py-2"><span>{e.description}</span><b>${(e.amount_cents/100).toFixed(2)}</b></div>)}<h3 className="font-bold">Who owes whom</h3>{payments.length === 0 ? <p className="text-slate-600">Everyone is settled.</p> : payments.map((p) => <p key={`${p.from}-${p.to}-${p.amountCents}`} className="rounded-xl bg-slate-100 p-3"><b>{p.from}</b> owes <b>{p.to}</b> ${(p.amountCents/100).toFixed(2)}</p>)}</div>; }
