import { addExpense } from "../lib/actions/expenses";
export function ExpenseForm({ tripId, memberIds }: { tripId: string; memberIds: string[] }) { return <form action={addExpense} className="card space-y-3"><h2 className="text-xl font-bold">Log expense</h2><input type="hidden" name="trip_id" value={tripId}/><input type="hidden" name="split_users" value={memberIds.join(",")}/><input className="input" name="description" placeholder="Dinner, tickets, taxi..." required/><div className="grid gap-2 md:grid-cols-2"><input className="input" name="amount" type="number" min="0.01" step="0.01" placeholder="Amount" required/><input className="input" name="spent_at" type="date" required/></div><p className="text-sm text-slate-600">Splits evenly across current trip members.</p><button className="btn w-full">Save expense</button></form>; }
