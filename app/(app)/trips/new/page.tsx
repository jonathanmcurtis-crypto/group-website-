import { redirect } from "next/navigation"; import { AppNav } from "../../../../components/app-nav"; import { createTrip } from "../../../../lib/actions/trips"; import { getUser } from "../../../../lib/supabase/server";
export default async function NewTripPage() { const user = await getUser(); if (!user) redirect("/login"); return <><AppNav email={user.email}/><main className="mx-auto max-w-2xl p-6"><form action={createTrip} className="card space-y-4"><h1 className="text-3xl font-black">Create a trip</h1><input className="input" name="name" placeholder="Trip name" required/><input className="input" name="destination" placeholder="Destination" required/><div className="grid gap-3 md:grid-cols-2"><input className="input" name="start_date" type="date" required/><input className="input" name="end_date" type="date" required/></div><button className="btn">Create trip</button></form></main></>; }
