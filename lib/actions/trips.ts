"use server";
import { redirect } from "next/navigation";
import { createSupabaseServerClient, getUser } from "../supabase/server";
export async function createTrip(formData: FormData) { const user = await getUser(); if (!user) redirect("/login"); const supabase = createSupabaseServerClient(); const { data, error } = await supabase.from("trips").insert({ name: String(formData.get("name")), destination: String(formData.get("destination")), start_date: String(formData.get("start_date")), end_date: String(formData.get("end_date")), created_by: user.id }).select("id").single(); if (error) throw new Error(error.message); await supabase.from("trip_members").insert({ trip_id: data.id, user_id: user.id, role: "owner" }); redirect(`/trips/${data.id}`); }
export async function inviteMember(formData: FormData) { const supabase = createSupabaseServerClient(); const email = String(formData.get("email")); const trip_id = String(formData.get("trip_id")); const { data: profile } = await supabase.from("profiles").select("id").eq("email", email).single(); if (!profile) throw new Error("No account exists for that email yet."); await supabase.from("trip_members").upsert({ trip_id, user_id: profile.id, role: "member" }); }
